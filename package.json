{
  "name": "ez-send",
  "version": "1.2.4",
  "description": "Send emails in Node.js with a single function. A simple-to-use abstraction that sits on top of Nodemailer - for simple email use cases.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "email",
    "send",
    "nodemailer"
  ],
  "author": "Ibrahim PG",
  "license": "MIT",
  "dependencies": {
    "nodemailer": "^6.7.2"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.4",
    "typescript": "^4.6.2"
  }
}
